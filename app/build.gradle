apply plugin: 'com.android.application'

android {
    signingConfigs {
        DefaultSigning {
            keyAlias 'standard'
            keyPassword 'Way23sonleR'
            storeFile file('C:/Users/Philipp/OneDrive/AndroidStudio/AndroidStudioKeystore/keystore.jks')
            storePassword 'Way23sonleR'
        }
    }
    compileSdkVersion 25
    buildToolsVersion "25.0.0"
    defaultConfig {
        applicationId "de.derappkoch.nfcextendedlengthfix"
        minSdkVersion 19
        targetSdkVersion 25
        versionCode 2
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    provided 'de.robv.android.xposed:api:82'
}

